一／基本查询
find()      查找所有记录
findOne()   只查找一条

find().pretty()   ＃pretty可以将结果格式化

二 比较运算符
    等于  默认就是等于判断没有运算运算符..
    小于  $lt
    小于或等于 $lte
    大于   $gt
    大于或等于  $gte
    不等于  $ne

    find({name:'wade'})  等于

    find({age:{$gt:34}})
