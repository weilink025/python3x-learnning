import MySQLdb

class mysqlHelper():
    #init方法
    def __init__(self,host,port,db,user,passwd,charset):
        self.host = host
        self.port = port
        self.db = db
        self.user = user
        self.passwd = passwd
        self.charset = charset

    #连接数据库，创建cusor

    #查一条

    #查多条

    #增

    #删

    #改