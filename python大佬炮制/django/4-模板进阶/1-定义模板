模板语言包括
    变量 {{value}}
    标签 {% 代码 %}
    过滤器
    注释 {# 代码或html #}

变量
    语法
        {{value}}
        变量必须由字母、数字、下划线和点组成
        当模版引擎遇到点（'.'）,会按照下列顺序查询   #{{ foo.bar }}
            1 字典查询 例如：foo['bar']
            2 属性或方法查询  例如：foo.bar
            3 数字索引查询    foo[bar]

        如果变量不存在，模版系统将插入空字符串
        在模板中调用方法时不能传递参数  不能有小括号（）


