定义属性时，需要字段类型

字段类型被定义在django.db.models目录下，为了方便，被导入到django.db.models中

使用方式：
    导入from django.db import models
    通过models.Field创建字段类型的对象，赋值给属性

对于重要数据都做逻辑删除，不做物理删除，实现方法是定义isDelete属性，类型为BooleanField,默认值为False