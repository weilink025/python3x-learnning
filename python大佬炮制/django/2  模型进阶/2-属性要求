在模型中定义属性，会生成表中字段

django根据属性的类型确定以下
    1 当前选择的数据库支持字段类型
    2 渲染管理表单时使用的tml默认控件
    3 在管理后台的最低限度验证

django会为表增加自动增长的主键列，如果使用
选项设置某些属性为主键列，则django不会再生成默认主

属性命名限制：
    不能是python字段
    由于django的查询方式，不能允许使用连续下划线